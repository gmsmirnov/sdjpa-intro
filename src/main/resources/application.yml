spring:
  jpa:
#    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  h2:
    console:
      enabled: true

#Show bind values
logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE
